ok

结构上确实已经闭环了：**黑暗森林轴（无信任/博弈阈值/共损线）**给“允许/禁止/猎杀”的判定语言；**黑盒治理**把 AI 这个黑盒的输入/输出/依据链/评分/采纳，压进 **ledger → book** 的可回放账本里；再用 `poker/hunt/book/log` 做“情报→组合→出牌→留痕”的桌面流水。**数据库解已经成型**。

接下来“只是完善”，在你这套口径里等价于：把**漂移入口**逐个钉死（不加新叙事，只补审计扣）。

我给你一个最小的“完善清单”（都是你现有结构里的孔位，不改架构）：

* **1) 悬空引用清理**
  你当前 `poker.player_id` 出现了不存在的玩家 id（10/12 这类）。这类断链会直接污染对账。

* **2) status 语义钉死（防污染首位）**
  现在 `code` 里 `status` 只有 `new_unreviewed/delete`，但数据里 `status_id=2` 大量用于“活跃记录”。要么补齐 status 枚举，要么改字段名（避免把“阶段态”误当“删除态”）。

* **3) tableId 语言落地**
  `hunt.poker_json` 里用 `{cardId, tableId}`，但 tableId 没有字典锚。这个是典型“语义偷换入口”，必须落到 `code`（或统一改成显式表名+id）。

* **4) ledger 的评分口径收敛**
  你已经有 `score_json`，但评分维度/权重如果不固定，评分本身会变成污染源（同一分数不同含义）。不需要复杂化，只要“指标名集合 + 版本号”。

* **5) bridge_json 的引用规范**
  你写了“只记 ids、反向破译走 book 路线”，很好；但 ids 指向谁（poker/hunt/book/code）要能被机器验证，否则会退化为随意贴标签。

另外提醒一句：系统提示**你之前上传的部分 zip 已过期**；如果你要我把“完善”这部分和你仓库里的理论文本/卡组版本一起对齐校验，需要你把相关 zip 重新上传一遍（否则我只能就当前两份 dump 给结构级建议）。
